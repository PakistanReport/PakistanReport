# Live verification — 12 September 2026

**Partial result, stopped at the model boundary. No deployment or public publishing.**

## What ran

The existing SourceClient performed real HTTP/robots checks against eleven institutional/reporting endpoints, followed by two legitimate fallback endpoints. The successful local Node run ingested 38 observations: PBS RSS 10, Dawn RSS 26, FBR HTML index 2. One failed source did not stop subsequent probes. A separate proxy-enabled run timed out; successful default-transport retrieval is therefore not evidence of transport-independent reliability.

Native local workerd could not resolve PID (DNS failure). Actual workerd HTTP/SQLite subsequently accepted the fresh live captures and retrieved the PBS primary document through a real Node outbound bridge. This verifies application runtime behavior, **not Cloudflare-hosted networking**. Nothing was deployed.

See `2026-09-12.json` for endpoints, HTTP responses, failed attempts, candidate scores/factors, provenance hashes and final-code replay results. Source registry entries remain disabled. No restrictions were bypassed.

## Source health and initial coverage

| Area | Tested source | Actual result / legitimate fallback |
|---|---|---|
| Economy | PBS feed | 200; 10 parsed items; current weekly price release; primary document also 200 |
| Economy | SBP | robots endpoint 403; stopped. Dawn is discovery only, not substitute primary evidence |
| Economy | Finance Division | timeout; Dawn discovered a fuel-price lead, still needs primary notification |
| Business | FBR | 200; two links, neither publishable; regulatory index requires specific documents |
| Business | SECP feed | timeout locally; web lookup resolved feed URL to homepage, so not a verified RSS endpoint; manual disclosure evidence remains an option |
| Pakistan / multiple beats | Dawn | 200; 26 headlines/links; no newspaper full-text crawl |
| Pakistan / Politics | PID | local timeout; web research readable, not connector success |
| Politics | National Assembly | root redirected; reviewed canonical `/en/index.php` also timed out |
| Jobs | FPSC; PBS job-openings fallback | both timed out in connector run; PBS web page reported no openings |
| Technology | PTA | timeout; no dedicated reliable connector established |
| World affecting Pakistan | MOFA | robots endpoint 502; no dedicated reliable connector established |

This is **not yet a complete usable seven-beat registry**. Courts and PSX remain manual intake, not verified live connectors. Existing Dawn discovery can supply leads across beats, but source-level category defaults are coarse and need editor correction.

## Editorial results

Final-code offline evaluation of the actual captures produced 38 separate candidates, five advancing for research. These are research decisions, not verified stories or approval:

- PBS weekly price index: Economy; score 100; LOW. A measured 0.23% weekly change, index 364.26, week ending 10 September. Primary landing page obtained. Score saturation overstates precision/significance: a modest weekly move should not automatically outrank major national decisions.
- Dawn fuel-price increase report: score 68; NORMAL. Reported petrol/diesel increases; primary notification and corroboration outstanding.
- Dawn pilgrim vaccination-certificate requirement: score 93; NORMAL. Reported new requirement/deadline; underlying directive outstanding.
- Dawn allowance approval for public employees in 26 KP districts: score 68; NORMAL. Reported implemented benefit; original notification outstanding.
- Dawn IHC restraint on coercive action against legal sheesha cafes: score 68; SENSITIVE. Original court order and legal context outstanding.

The last four retain the source's Pakistan category until editorial correction; recommended fuel-price category is Economy and the court story needs legal/political review. No reporting headline alone was treated as verified factual evidence.

Correct rejections included FBR's retirement notice and navigation link, a diplomatic credentials ceremony, a judiciary-strengthening speech, coalition reconciliation intentions, and stale PBS releases. Separately, live PID web results showed ceremonial/meeting material; those were web research, **not monitor discoveries**.

## Defects found and fixed

- Generic announcement/publication verbs and institutional authority could substitute for concrete change. Added a material-action requirement, stronger PR penalties and a current-date gate. Dates alone no longer count as economic measurements.
- Feed boilerplate merged separate PBS inflation/trade releases. Headline Jaccard agreement now constrains clustering. Final replay: 38 observations → 38 candidates; no real multi-source event cluster demonstrated in this sample. Replaying all 38 exact observations through actual workerd created no duplicates. Positive multi-source clustering remains deterministic-test evidence.
- Monitoring selected list order rather than highest significance score. Processing now prioritizes score; a failed source still permits candidate processing.
- Abbreviated parliamentary/judicial headlines missed sensitive classification. Expanded conservative triggers with regressions.

Remaining editorial limitations are material: keyword matching misses some consequential political actions, cannot reliably infer Pakistan impact from foreign events, and can saturate scores. It is a cautious triage aid, not demonstrated newsroom judgment. Human review remains essential.

## Drafting and visuals

Actual local workerd retained the PBS document (837 characters; hash and retrieval time in JSON), then returned **Needs Attention: model drafting disabled**. No model/API credential, endpoint, model name or allowed-host configuration exists here. No AI draft, article-quality evaluation, claim-level draft checks or Ready for Review outcome can honestly be reported. The page establishes the headline numbers only; it does not justify invented drivers, household consequences, predictions or a padded article. Detailed tables/context still need verification.

The existing visual-brief function ran on the real candidate: neutral, non-photographic conceptual artwork, no actual events/people/documents/seals or unsupported figures; caption **Pakistan Report illustration.** The brief is awaiting visual completion, not an image or a reviewed visual.

To resume real drafting, configure an already-authorized OpenAI-compatible JSON chat-completions provider in ignored `_newsroom/automated/.dev.vars`: `DRAFT_PROVIDER`, `MODEL_ENDPOINT`, `MODEL_NAME`, `MODEL_ALLOWED_HOSTS`, and secret `MODEL_API_KEY`. Do not put a key in Git or chat. No verified free live provider is available in this environment; an existing authorized free allocation may work if compatible. Deterministic fixture mode is free but cannot validate model quality. No service was purchased.

## Trial recommendation and future lanes

Suitable for a supervised, local shadow evaluation of accessible sources only; **not ready for a Cloudflare-hosted seven-beat shadow trial or restricted auto-publishing**. First establish hosted retrieval and evaluate real drafts. Existing risk, checks, revisions and audit records could inform later lane decisions, but no auto-eligible routing, auto-publication endpoint or kill-switch feature was added. All current outputs require human approval, and no public-write capability exists.

## Executed regression results

- Automated Newsroom: **52 passed, 0 failed, 0 skipped** (includes eight new materiality/clustering/risk/monitor regressions).
- Existing Publisher: **35 passed, 0 failed, 0 skipped**.
- Wrangler dry-run bundle succeeded; no deployment.
- Public site and Publisher paths remain identical to the starting commit.
